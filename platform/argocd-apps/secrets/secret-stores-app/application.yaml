apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secrets-secret-stores
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/ovsal/k8s-va.git
    targetRevision: main
    path: platform/argocd-apps/secrets/secret-stores
    directory:
      recurse: false
      include: 'vault-clustersecretstore.yaml'
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true

